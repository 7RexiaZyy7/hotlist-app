# 故障排查记录：Coze Agent → Vercel 链路（2026-05-23 中午 ~ 05-24）

## 背景

- 线上地址：`https://hotlist-app-one.vercel.app/`
- Bot ID: `7639197902187020297`（名称：热点获取）
- 认证方式：Coze PAT token（环境变量 `COZE_PAT_TOKEN`）
- 前端框架：Vite + React + TypeScript
- 后端：Vercel Serverless Function（`api/proxy.js`）

## 已知正常的工作版本

Commit `f3081b7`（2026-05-22）：
- 随机 `user_id`（`user_xxx`），不涉及 OAuth
- `auto_save_history: true`
- 轮询策略：先等 retrieve `completed`，再查 messages
- 所有 Coze API 调用用 PAT

## 排查过程

### 第一阶段：5/23 下午 — 引入 Coze OAuth

添加 PKCE OAuth 登录（commit `1966e0b`），目标是持久化 `user_id`。

主要改动：
- `api/proxy.js`：新增 OAuth endpoints（authorize / token / refresh / status / logout）
- `src/services/cozeApi.ts`：新增 OAuth PKCE 函数，`callCozeChat` 接受 `oauthUid` 参数

**问题出现**：OAuth 回调后 `setUserId(result.uid)` 将 `coze_user_id`（localStorage key）覆盖为真实 OAuth uid。

### 第二阶段：5/23 晚上 — auto_save_history 反复尝试

Coze API 的 `auto_save_history` 参数在 true/false 间来回切换：

| 时间 | Commit | 操作 | 结果 |
|------|--------|------|------|
| 21:47 | 173a078 | 改为先轮询 messages（不依赖 retrieve） | 失败 |
| 21:50 | be1e090 | PAT 建对话、OAuth token 查 retrieve/messages + auto_save_history=true | 失败 |
| 21:55 | eb0b8d0 | 恢复 auto_save_history=true | 失败 |
| 22:12 | 3d00a32 | fetchMessages 只返回 answer/final | 失败 |
| 22:21 | 34beb19 | 前端直调 uapis.cn 获取热榜，绕过 Coze Agent | 单平台热榜可用，但综合热榜不行 |
| 22:36 | 679188c | 全部用 PAT，OAuth 只用于身份 | 失败 |
| 22:59 | 6568a56 | `auto_save_history=false` | 失败 |
| 23:12 | d4c125f | stream/auto_save_history 用字符串值 | 失败 |
| 23:46 | 02138f8 | 去掉 auto_save_history（Coze 默认） | 失败 |
| 23:57 | f148f4b | **恢复工作版**：auto_save_history=true + 先等 retrieve 完成 + 消息多层 fallback | ⚠️ 用户测了仍不可用 |

### 第三阶段：5/24 凌晨 — 权限优先级翻转

Commit `6395709` 和 `9bf093c` 关键冲突：

- `9bf093c`（5/23 18:08）：**证明 OAuth token 缺少插件权限，必须用 PAT 调 Coze API**
- `6395709`（5/24 00:33）：**将权限优先级翻转为 OAuth cookie 优先 → PAT 兜底**

**这是根因**：虽然 `9bf093c` 已证明 OAuth token 不可用，`6395709` 又把优先级设回 OAuth 优先。
结果：有残存 OAuth cookie 的用户 → proxy 用 OAuth token 调 Coze API → OAuth token 缺插件权限 →
Bot 在 Coze 侧正常执行（Coze 日志有输出），但 retrieve 永远返回 `in_progress`。

### 第四阶段：5/24 上午 — 隔离 API user_id 与 OAuth uid

- `ea96a0c`：用 `coze_api_user_id` 作为 localStorage API key，OAuth uid 存 `coze_oauth_uid`
- `fe450f8`：给 callCozeChat 和 pollForResult 加日志
- `88a589f`：改为每轮先查 messages 再查 retrieve（不等待 retrieve completed）

**用户测试结果**：
- Console 显示 `pollForResult` 入口日志，但**因为不等待 retrieve completed，messages 永远只有 `verbose`（knowledge_recall 日志），没有 `answer`**
- 40 轮 × 3s = 120s 超时，最终 "获取响应超时"

### 第五阶段：5/24 下午 — 纠正消息匹配

- `0f033b7`：修正消息匹配条件，只匹配 `type === 'answer'`，去掉 `role === 'assistant'` 避免误匹配 knowledge_recall
- `1d0d7c7`：恢复为"先等 retrieve completed 再查 messages"

**用户测试结果**：
- retrieve 始终 `in_progress`，**即使 Coze 侧 Bot 实际已完成**
- ⚠️ **确认 BOT 日志显示成功输出，但 retrieve API 返回仍为 in_progress**
- 提示：此前 auth 优先级为 OAuth first（`6395709`），用户浏览器可能有残存 OAuth cookie 导致 token 错误

### 第六阶段：5/24 下午 — 最终修复

- `4af4c92`：**恢复 PAT-first 优先级**，所有 Coze API 调用强制用 PAT

**服务端 debug 端点验证**（`/api/proxy?action=debug`）：
```
retrieve: 11s → completed ✅
Bot output_count: 307 tokens ✅
messages: type=answer, content="你好！很高兴为你服务！..." ✅
follow_up: 3条 follow_up 消息 ✅
```

## 时间轴总结

```
[5/22] f3081b7 — 正常工作（PAT only, 无 OAuth）
[5/23 15:14] 1966e0b — 加入 OAuth PKCE
[5/23 17:27] 714d0c1 — PKCE token header 修复
[5/23 17:37] 952583f — 忽略无效 OAuth cookie
[5/23 18:08] 9bf093c — ⚠️ 发现 OAuth token 缺插件权限 → 改用 PAT（正确方向）
[5/23 21:47~23:57] 8 次 auto_save_history 切换 — 均失败
[5/23 23:57] f148f4b — 恢复工作版逻辑（仍不可用）
[5/24 00:33] 6395709 — ❌ 翻转到 OAuth first（与 9bf093c 矛盾）
[5/24 11:25] ea96a0c — 隔离 API/OAuth userId
[5/24 11:30] fe450f8 — 加日志
[5/24 12:00] 88a589f — 改轮询策略（不等待 retrieve）
[5/24 12:48] 0f033b7 — 修消息匹配
[5/24 12:54] 1d0d7c7 — 恢复"等待 retrieve"策略
[5/24 14:03] 4af4c92 — ✅ 恢复 PAT first（最终修复）
```

## 根因

1. **直接原因**：`6395709` 将 Coze API 认证优先级从 PAT-first 改为 OAuth-first，导致有残存 OAuth cookie 的用户请求全部走 OAuth token。
2. **根本原因**：Coze OAuth token 缺少执行 Bot 所需的插件权限（commit `9bf093c` 已验证），因此用 OAuth token 发起的对话在 Coze 侧无法写入插件执行结果，retrieve 永远卡在 `in_progress`。
3. **隐蔽点**：Coze Agent 日志显示 Bot 正常完成（因为 Bot 自身的推理和执行成功了），但对话状态未正确回写到 Coze API retrieve 端点，造成"Agent 有输出但前端收不到"的假象。

## 最终修复内容

1. `api/proxy.js`：Token 优先级从 `validCookieToken || COZE_PAT_TOKEN` 改为 `COZE_PAT_TOKEN || validCookieToken`
2. `src/services/cozeApi.ts`：恢复为"先等 retrieve completed → 再查 messages"的轮询策略，并添加完善的逐轮日志

## 后续检查

1. ✅ Deploy 后请**清除 `hotlist-app-one.vercel.app` 所有 cookie**（或开隐身窗口）后测试
2. ☐ [待用户确认] 确认综合热榜能正常返回数据（`retrieve completed → messages answer → parseHotList`）
3. ☐ [待用户确认] OAuth 登录用户在 PAT-first 下是否有功能缺失（理论上 PAT 权限已够）
