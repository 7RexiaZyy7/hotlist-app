# 会话记录

## 项目概况

**项目名**：内容引力引擎（hotlist-app）
**技术栈**：Vite + React + TypeScript + Tailwind CSS
**部署**：Vercel (Hobby 计划)
**后端依赖**：Coze API（Bot ID: 7639197902187020297）、uapis.cn（热榜数据）

---

## 页面结构

| 页面 | 路由 | 功能 |
|------|------|------|
| 热榜驾驶舱 (HotRadar) | radar | 综合热榜（Coze Bot）+ 单平台热榜（直调 uapis.cn，<1s） |
| 话题勘探 (TopicExplorer) | topic | 跨平台话题搜索 |
| 文案工坊 (ContentForge) | forge | 11 种文案角度多版本生成 |
| 爆款拆解 (HitAnalyzer) | analyze | 爆款文案分析 + 智能洗稿 |
| 创作档案 (CreatorProfile) | profile | 赛道/受众/风格偏好管理 |

---

## 已完成功能

### 热榜
- [x] 综合热榜 Top15（走 Coze Bot，但 v2ex 接口已废，用户已改 Bot 提示词为 juejin）
- [x] 单平台热榜（微博/抖音/知乎/B站/V2EX）直调 uapis.cn
- [x] 热榜数据本地缓存
- [x] 点击热榜话题跳转到文案工坊

### 文案工坊
- [x] 11 种文案角度选择（共鸣型/知识型/观点型/趣味型/实用型/关联型/决策纠结型/问题原因型/填补盲区型/替用户说话/行业发心型）
- [x] 多角度同时生成
- [x] 按角度拆分结果展示
- [x] 一键复制
- [x] Bot 反问兜底检测（askingInfo）

### 爆款拆解
- [x] 五部分分析（钩子分析/结构拆解/关键元素/可复用模型/风险提示）
- [x] 结构表格渲染（markdown 表格 → HTML table）
- [x] normalizeHeader 兼容 Bot 各种输出格式（1. 钩子分析 / 【钩子分析】等）
- [x] 智能洗稿（选择风格后改写）
- [x] Bot 反问兜底检测

### 创作档案
- [x] 赛道、受众、昵称、文风、体裁管理
- [x] 数据持久化到 localStorage
- [x] 通过 custom_variables 同步到 Coze Bot

### 知识库
- [x] `coze-knowledge/爆款文案框架完全指南.md` — AIDA/PAS/FAB/4P/BAB/PPP/Pain-Agitate-Solution/特征-优势-利益-证据框架
- [x] `coze-knowledge/中文社交媒体爆款文案模板.md` — 各平台标题公式、正文模板、行业模板
- [x] `coze-knowledge/AI提问技巧.xlsx`
- [x] `scripts/coze-knowledge-setup.mjs` — 上传脚本
- [x] 知识库已在 Coze Web UI 绑定到 Bot

### 部署 & API
- [x] api/proxy.js — Vercel serverless 代理（hotboard/chat/retrieve/messages/variables/debug）
- [x] proxy.ts → proxy.js（Vercel 不支持 TS）
- [x] 去掉 vercel.json 的 functions 配置修复部署
- [x] 前端直调 Coze API（绕过 proxy 的 chat/retrieve/messages）
- [x] PAT token 配到 Vercel 环境变量（VITE_COZE_TOKEN）
- [x] 综合热榜走 Coze Bot（慢），单平台直调 uapis.cn（快）
- [x] 去掉服务端轮询（Vercel Hobby 10s 超时限制）

### UI/UX
- [x] Toast 通知替代 alert
- [x] 空状态引导
- [x] 脉冲动画引导点击平台标签
- [x] 布局压缩给结果区更多空间

---

## Coze OAuth 已实现

- [x] `api/proxy.js` — 新增 OAuth 端点（oauth_authorize/oauth_token/oauth_refresh/oauth_status/oauth_logout/get_token）
- [x] `api/proxy.js` — 401 自动刷新 access_token（用 cookie 中的 refresh_token）
- [x] `api/proxy.js` — 所有 Coze API 调用优先使用用户 OAuth token，未登录则降级到 PAT
- [x] `scripts/dev-proxy.mjs` — 本地开发代理服务器（端口 3001）
- [x] `src/services/cozeApi.ts` — OAuth 工具函数（PKCE 挑战生成、登录/回调/刷新/登出）
- [x] `src/services/cozeApi.ts` — 所有 Coze API 调用改为经过 proxy（不再直接调用 Coze）
- [x] `src/store/index.ts` — 添加认证状态（isLoggedIn/cozeUid/accessToken/isLoadingAuth）
- [x] `src/pages/AuthCallback.tsx` — OAuth 回调处理页面
- [x] `src/App.tsx` — 启动时检查登录状态（自动刷新 token），回调路由
- [x] `src/components/TopBar.tsx` — 右上角登录/登出按钮
- [x] `vite.config.ts` — 本地开发代理配置（/api/proxy → localhost:3001）
- [x] `package.json` — 新增 dev:proxy / dev:all 脚本

### OAuth 流程
1. 用户点击「Coze 登录」→ 前端生成 PKCE code_verifier + code_challenge
2. 跳转到 Coze 授权页面 → 用户授权
3. Coze 回调到 `/auth/callback?code=xxx` → 前端提取 code
4. 前端调 proxy 的 `oauth_token` 接口 → proxy 用 code + verifier 换 access_token + refresh_token
5. Proxy 在 HTTP-only cookie 中存储 token → 前端拿到 access_token 存 zustand
6. 所有后续 Coze API 调用走 proxy → proxy 从 cookie 取用户 token

### 免重新登录
- refresh_token 存在 HTTP-only cookie 中（30 天有效期）
- 再次打开页面时 → App 启动调 `oauth_status` → proxy 自动用 refresh_token 刷新

### 需要配置
- Vercel 环境变量新增：`COZE_CLIENT_ID = 13649532017216334435107873770562.app.coze`
- Coze 控制台回调 URI：`https://hotlist-app-one.vercel.app/auth/callback` + `http://localhost:5173/auth/callback`

---

## 当前问题

### 1️⃣ 生成文案质量差（最核心）
**表现**：文案工坊生成的文案质量不理想
**根因**：`buildCopyGenerateQuery` 中没有指令让 Bot 使用知识库。知识库已绑定但 Bot 不会主动查
**修复方向**：在查询中加入 `【参考】从知识库中调用爆款文案框架和模板` 指令

### 2️⃣ 综合热榜有时不通
**表现**：综合热榜依赖 Coze Bot，Bot 插件中 v2ex 接口已失效
**状态**：用户已在 Coze Web UI 将 Bot 提示词中 3 处 v2ex 改为 juejin
**验证**：需重新发布 Bot 后测试

### 3️⃣ Bot 提示词优化
**表现**：Bot 有时会反问"请提供完整文案"或"请先填写创作档案"
**修复**：
- 查询中加了"不要问任何问题，没有赛道/受众直接写通用文案"
- 前端加了 askingInfo 检测 + 友好提示
- 更彻底的修复需要去 Coze 工作流中关闭前置判断节点

---

## 架构说明

```
浏览器 (React) 
  ├── → Coze API (api.coze.cn) ─── 聊天/轮询/获取消息（直接调用，不走 proxy）
  ├── → uapis.cn ───────────────── 单平台热榜（走 Vercel proxy）
  └── → /api/proxy?action=debug ── 连接诊断（走 Vercel proxy）

Vercel proxy (api/proxy.js)
  ├── action=hotboard → uapis.cn
  ├── action=debug → Coze API（连通性测试）
  └── 其余 action（chat/retrieve/messages/variables）前端不再经过 proxy
```

---

## 关键配置

- Coze Bot ID: `7639197902187020297`
- Coze Space ID: `7621770081843871763`
- PAT token: 已配 VITE_COZE_TOKEN（已验证可用）
- API Base: `https://api.coze.cn`
- uapis.cn: `https://uapis.cn/api/v1/misc/hotboard?type=weibo`（免费，无需 key）
- Git remote: `https://github.com/7RexiaZyy7/hotlist-app.git`

---

## 待办

- [ ] 在查询中加知识库引用指令（改善文案质量）
- [ ] 验证综合热榜 juejin 修改后是否正常
- [ ] 重新发布 Coze Bot
- [ ] 优化 Bot 提示词（精简规则 + 加少样本示例）
